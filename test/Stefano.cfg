process CnBAnalyzer = {
    service = DaqMonitorROOTBackEnd{}

    # message logger
    # include "DQM/SiStripCommon/data/MessageLogger.cfi"

    # service = MessageLogger 
    # {
    # untracked vstring destinations   = { "debugmessages.txt" }
    # untracked vstring categories     = { "FEDBuffer" }
    # untracked vstring debugModules   = { "*" }
    # untracked PSet debugmessages.txt = { untracked string threshold = "DEBUG"
    # untracked PSet INFO            = { untracked int32 limit = 0 }
    # untracked PSet DEBUG           = { untracked int32 limit = 0 }
    # untracked PSet interestingToMe = { untracked int32 limit = 10000000}
    # } 
    # }

    include "DQM/SiStripMonitorHardware/test/rib30148Castor.cfi"
    untracked PSet maxEvents = { untracked int32 input = 100 }

    module CnBTest = CnBAnalyzer
    {         
	untracked int32 swapOn = 1 // non zero value does the DAQ header offset, etc.
	untracked int32 N = 10 // if the above is true, then "N" events before you update % error hitograms
	untracked int32 runNumber = 10238
	untracked string rootFile = "FED_DQM_Data_Stefano.root"
    }

    path p = { CnBTest }
}
